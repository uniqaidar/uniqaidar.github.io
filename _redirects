# UniQaidar — Cloudflare Pages redirects

/robots.txt              /robots.txt              200
/fonts.json              /fonts.json              200

# Sitemap rewrites — map the real URL to the Function's clean route
# The Function files cannot have dots in their names, so we use hyphens
# and rewrite the canonical .xml URLs here with 200 (transparent rewrite).
/sitemap.xml             /sitemap-xml             200
/sitemap_index.xml       /sitemap-index-xml       200
